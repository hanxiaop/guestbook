apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: staging
  namespace: xp-0821-guestbook
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  shard: xp-0821-agent
  requestedFreight:
    - origin:
        kind: Warehouse
        name: guestbook
      sources:
        stages: [dev]
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: https://github.com/hanxiaop/guestbook.git
            checkout:
              - branch: main
                path: ./repo
        - uses: yaml-update
          config:
            path: ./repo/xp-0821/staging/deployment.yaml
            updates:
              - key: spec.template.spec.containers.0.image
                value: ${{ imageFrom("ghcr.io/akuity/guestbook").RepoURL }}:${{ imageFrom("ghcr.io/akuity/guestbook").Tag }}
        - uses: git-commit
          config:
            path: ./repo
            message: Promote guestbook ${{ imageFrom("ghcr.io/akuity/guestbook").Tag }} to staging
        - uses: git-push
          as: push
          config:
            path: ./repo
        - uses: argocd-update
          config:
            apps:
              - name: xp-0821-guestbook-staging
                sources:
                  - repoURL: https://github.com/hanxiaop/guestbook.git
                    desiredRevision: ${{ outputs.push.commit }}
